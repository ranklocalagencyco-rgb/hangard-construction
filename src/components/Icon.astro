---
interface Props {
  name: string;
  class?: string;
}
const { name, class: className = "" } = Astro.props;
---
<span class:list={["material-symbols-outlined", className]} aria-hidden="true">{name}</span>
